import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'confraria-do-temaki',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
